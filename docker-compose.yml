name: mcpgw

services:
  airegistry:
    image: golang:1.26
    restart: always
    working_dir: /app
    command: sh -c "go mod download && go run ."
    volumes:
      - ./:/app
    env_file:
      - .env
    environment:
      DBHOST: db
    ports:
      - "9098:9098"
